        <?=

         app\widgets\mediaPopup\MediaPopup::widget([
             => $model->imageOverviewCamera->url,
            'type' => $model->imageOverviewCamera->file_type,
        ])

        app\widgets\mediaPopup\MediaPopup::widget([
             'url_2' => $model->imageOverviewCamera->url,
             'type_2' => $model->imageOverviewCamera->file_type,

        ])



        ?>
